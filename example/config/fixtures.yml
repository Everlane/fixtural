
allow_tables:
  - events
  # - products
  - reports

disallow_tables:
  - users

output:
  store: local
  path:  'test/fixtures'

environments:
  admin:
    output:
      store: S3
      access_key_id: 'foo'
      secret_access_key: 'bar'
      path: 'fixtures'

